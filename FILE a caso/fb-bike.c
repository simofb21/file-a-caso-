#include <stdio.h>
#include <unistd.h>

int main() {
    while (1) {
        printf("Sei stato hackerato da fb-bike.it \nVisita sito per togliere questo problema\n");
        sleep(1); // Intervallo di 1 secondo (puoi modificare il valore secondo le tue esigenze)
    }

    return 0;
}
